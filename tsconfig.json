{
  "compilerOptions": {
    "target": "ES2020", // Update target to ES2020 for more modern features
    "module": "ESNext", // Use ESNext module syntax for better compatibility with modern frameworks
    "outDir": "./dist/code", // Output directory for compiled JavaScript files
    "rootDir": "./src/code", // Root directory of your source files
    "strict": true, // Enable all strict type-checking options
    "sourceMap": true, // Generate source maps for easier debugging
  },
  "include": [
    "src/code/**/*" // Include all TypeScript files in the src directory
  ],
  "exclude": [
    "node_modules", // Exclude the node_modules directory
    "dist" // Exclude the dist directory
  ]
}